Project,Issue,Commit,TypeChecker,Detectable,Comment,AnnotationTime
beefsack/node-hound,3,c6f13b31808b5f0be2dc63946c7fd25841a387e4,Flow,Detectable,"this was not caught in Flow because apparently flow includes the node.js api and the version flow uses doesn't fail the type check.  But if it had the new version, it would catch the error.",0.0
